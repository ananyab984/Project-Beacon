# Test-only dependencies -- kept separate from requirements.txt so pytest
# and its plugins never become a runtime dependency of the deployed service.
-r requirements.txt
pytest>=8.0.0
pytest-mock>=3.12.0
responses>=0.25.0
pytest-cov>=5.0.0
